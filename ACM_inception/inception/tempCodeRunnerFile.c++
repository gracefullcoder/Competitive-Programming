if (arr[i] % k != 0 && (i + 1) % k == 0)
            // {
            //     for (int l = i + k; l < n; l = k + n)
            //     {
            //         if (arr[l] % k == 0)
            //         {
            //             count++;
            //         }
            //     }
            // }